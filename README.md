# Shamir Secret Solver

This project reconstructs the hidden secret using Shamir Secret Sharing polynomial interpolation.

## Features
- Supports very large numbers using BigInt
- Converts arbitrary base values
- Uses Lagrange interpolation
- Requires only k points

## Run
node solver.js
